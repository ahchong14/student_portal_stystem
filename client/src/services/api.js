// Use mock API for demo purposes
// Change this back to the real API when ready: 
// import axios from 'axios';
// const api = axios.create({...})

import mockApi from './mockApi';

const api = mockApi;

export default api;
